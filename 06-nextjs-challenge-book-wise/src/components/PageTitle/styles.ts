import { styled } from '@/../stitches.config'

export const Container = styled('div', {
  display: 'flex',
  alignItems: 'center',
  gap: '$4',
  marginBottom: 40,

  svg: {
    color: '$green100',
  },
})

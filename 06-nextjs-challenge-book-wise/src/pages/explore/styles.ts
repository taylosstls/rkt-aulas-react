import { styled } from '@/../stitches.config'

export const ExploreContainer = styled('section', {
  width: '100%',
  height: '100%',
  overflow: 'hidden',
  display: 'flex',
  flexDirection: 'column',
})
